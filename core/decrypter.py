"""
PS3 ISO Decryption (DISABLED)
==============================
This feature has been removed. PS3 ISO decryption is a manual process
that the user performs outside of Game Machine using RPCS3 directly.
Game Machine now launches PS3 ISOs directly like all other emulators.
"""

# All PS3 encryption/decryption logic has been intentionally removed.
# Users must decrypt their PS3 ISOs manually before placing them in the
# RPCS3 ROMs folder. Game Machine will launch them as-is.